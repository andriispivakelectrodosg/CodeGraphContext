# Progress

## What's Working
- **Fork**: andriispivakelectrodosg/CodeGraphContext on GitHub, cloned locally
- **10 custom fixes** merged to `main` (squash commit `f92655e`):
  - Dead code query checks all edge types (CALLS, INSTANTIATES, IMPORTS, INHERITS, USES)
  - Anonymous callback parent context walking (resolves symbols in callbacks to nearest named parent)
  - SCIP fallback indexing with scope, kind, and env variable support
  - .js → .ts file resolution in TypeScript and JavaScript parsers
  - `--scip` CLI flag and protobuf dependency
- **14 language parsers**: Python, JavaScript, TypeScript, Java, C, C++, C#, Go, Rust, Ruby, PHP, Swift, Kotlin, Dart, Perl (+ Scala, Elixir, Haskell)
- **3 database backends**: KùzuDB (default), FalkorDB (Lite + Remote), Neo4j
- **CLI toolkit**: index, analyze (callers, callees, tree, complexity, dead-code, calls), find, list, watch, mcp
- **MCP server**: 16+ tools for AI agent code graph queries
- **Bundle system**: Pre-indexed `.cgc` bundles for popular repositories
- **File watcher**: Live graph updates on file changes
- **Visualization**: Interactive HTML graph explorer

## What Remains
- [ ] Set up local venv with `[scip]` extras
- [ ] Test SCIP indexing end-to-end on a real project
- [ ] Sync with upstream for new features/fixes
- [ ] Improve tree-sitter parser accuracy for edge cases
- [ ] Consider upstreaming our fixes via PR
- [ ] Set up CI/CD for the fork
- [ ] Expand test coverage for SCIP integration

## Known Issues
- Git push requires MCP GitHub API tools (user216 has no CLI push access to fork org)
- SCIP indexing requires external SCIP index file (generated by language-specific indexers like scip-typescript)
- FalkorDB Lite only works on Unix with Python 3.12+
- Some tree-sitter parsers may miss symbols in complex code patterns (callbacks, decorators, dynamic dispatch)

## Overall Status
Phase: Fork established with improvements, local development environment setup pending
Completion: Fork + fixes 100%, local dev environment ~50% (cloned but venv not set up)
